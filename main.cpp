#include <stdio.h>  //标准输入输出
#include <math.h>
//*p1=*p2  p1=p2
int main() {
    /**
     * *p1=*p2是把p1指向的变量的值改变，但p1所指向的变量不变.
     *  p1=p2，则是p1指向改变了，即p1不再指向原变量的地址，改为指向p2所指向的地址。
     */
    return 0;
};

